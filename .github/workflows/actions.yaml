name: actions
on: 
    push
jobs:
    checkout-actions:
        runs-on: ubuntu-latest
        steps: 
            - name: checkout actions
              uses: actions/checkout@v4
            
            - name: current directory
              run: pwd 
